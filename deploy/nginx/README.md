# Nginx

Production reverse proxy configuration will live here when TLS, static asset serving, and API routing are finalized.

Phase 1 serves the frontend through `frontend/nginx.conf` inside the frontend container.

